# Pass-through Authentication Environment Variables

# Required
MCP_API_KEY="your-mcp-api-key"

# Optional - for service account fallback
GITHUB_SERVICE_TOKEN="ghp_serviceAccountToken"
SONARQUBE_SERVICE_TOKEN="sqp_serviceAccountToken"
JIRA_SERVICE_TOKEN="jira_serviceAccountToken"
ARTIFACTORY_SERVICE_TOKEN="art_serviceAccountToken"

# Optional - for credential storage encryption
CREDENTIAL_ENCRYPTION_KEY=$(openssl rand -base64 32)

# Database connection (for migrations)
DATABASE_URL="postgres://user:password@localhost:5432/mcp_db?sslmode=disable"

# Test credentials (for integration tests only)
TEST_GITHUB_PAT="ghp_testPersonalAccessToken"
TEST_JIRA_TOKEN="jira_testToken"