package core

import (
	"testing"
)

// S3 context manager tests are skipped as the S3 storage is not critical for the core server functionality
func TestS3ContextManager(t *testing.T) {
	t.Skip("S3 context manager tests are skipped as they are not critical for the core server functionality")
}
