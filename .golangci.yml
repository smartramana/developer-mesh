# This file configures golangci-lint
# Run with: golangci-lint run

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - unused
    - errcheck

issues:
  exclude-dirs:
    - .claude
    - vendor
    - .git
    - test/integration