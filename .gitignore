# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment files
.env
.env.*
!.env.example
!.env.*.example

# Local configuration overrides
configs/*.local.yaml
configs/config.*.local.yaml

# Claude configuration
CLAUDE.md   

# Built binaries
/mcp-server
/mockserver
/migrate
/new-mcp-server
/server
/simplified-mcp
/api
/apps/rest-api/rest-api
/apps/rest-api/rest-api-linux
/apps/rest-api/api
/apps/mcp-server/mcp-server
/apps/mcp-server/server
/apps/worker/worker
/apps/mockserver/mockserver
/bin/
# Any file without extension in app directories (likely binaries)
/apps/*/[!.]*
!/apps/*/Dockerfile
!/apps/*/Makefile


# Log files
*.log

# Functional test artifacts
logs/
.test-pids

# Development certificates
certs/
*.pem
*.p12
*.jks
*.crt
*.key
*.csr
